<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="payment_link_wizard_view_form" model="ir.ui.view">
        <field name="name">payment.link.wizard.form</field>
        <field name="model">payment.link.wizard</field>
        <field name="inherit_id" ref="whatsapp_payment.payment_link_wizard_view_form"/>
        <field name="arch" type="xml">
            <button name="action_send_whatsapp" position="attributes">
                <attribute name="invisible">not can_send_whatsapp or warning_message or context.get('using_paymob') == True</attribute>
            </button>
        </field>
    </record>

    <record id="action_invoice_order_generate_paymob_payment_link" model="ir.actions.act_window">
        <field name="name">Generate a Paymob Payment Link</field>
        <field name="res_model">payment.link.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="payment.payment_link_wizard_view_form"/>
        <field name="context">{'using_paymob': True}</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">form</field>
    </record>

</odoo>
